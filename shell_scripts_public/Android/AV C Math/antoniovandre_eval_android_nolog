#!/bin/bash

# Proprietário: Antonio Vandré Pedrosa Furtunato Gomes.

# Script lançador do "antoniovandre_eval_android" sem log.

# Última atualização: 15-04-2021. Não considerando alterações em variáveis globais.

if [ "${#}" == "1" ]; then antoniovandre_eval_android "${@}" "0"; else echo -e "${AV_VERMELHO} Número incorreto de argumentos${AV_SEMCOR}."; exit 1; fi
